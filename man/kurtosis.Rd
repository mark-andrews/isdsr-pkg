% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptives.R
\name{kurtosis}
\alias{kurtosis}
\title{Sample kurtosis (raw by default)}
\usage{
kurtosis(x, na.rm = TRUE, excess = FALSE)
}
\arguments{
\item{x}{Numeric vector.}

\item{na.rm}{Logical; if \code{TRUE} (default) remove \code{NA} values
before computing the statistic.}

\item{excess}{Logical; if \code{TRUE} subtract 3 so the normal
distribution maps to 0.  Defaults to \code{FALSE} (raw kurtosis).}
}
\value{
A single numeric value: raw kurtosis by default, excess
kurtosis when \code{excess = TRUE}.  Returns \code{NA_real_} if the vector
has fewer than four non‐missing observations.
}
\description{
Calculates the sample \emph{raw} kurtosis of a numeric vector using
\code{\link[moments:kurtosis]{moments::kurtosis()}}.  If you want \emph{excess} kurtosis
(raw – 3) set \code{excess = TRUE}.
}
\details{
\emph{Raw} kurtosis equals 3 for a normal distribution.
\emph{Excess} kurtosis is raw – 3, so the normal benchmark is 0.\tabular{lll}{
   Excess value \tab Tail description \tab Raw value \cr
   > 0 \tab heavy‐tailed \tab > 3 \cr
   = 0 \tab normal‐tailed \tab = 3 \cr
   < 0 \tab light‐tailed \tab < 3 \cr
}
}
\examples{
library(dplyr)

# Raw kurtosis (normal ≈ 3)
summarise(whr2025,
  raw_kurt_happiness = kurtosis(happiness),
  raw_kurt_gdp       = kurtosis(gdp)
)

# Excess kurtosis (normal ≈ 0)
summarise(whr2025,
  excess_kurt_happiness = kurtosis(happiness, excess = TRUE),
  excess_kurt_gdp       = kurtosis(gdp, excess = TRUE)
)

}

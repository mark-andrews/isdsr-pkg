% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressions.R
\name{tidy_anova_ssq}
\alias{tidy_anova_ssq}
\title{Tidy ANOVA sums-of-squares table}
\usage{
tidy_anova_ssq(model, type = c("II", "I", "III"), ...)
}
\arguments{
\item{model}{An object of class \code{\link{aov}} or \code{\link{lm}}.}

\item{type}{Character string specifying the sums-of-squares type:
\code{"I"}, \code{"II"}, or \code{"III"} (case-insensitive).
Defaults to \code{"II"}, in line with many applied-statistics conventions.}

\item{...}{Additional arguments passed to \code{\link[car:Anova]{car::Anova()}} when
\code{type} is \code{"II"} or \code{"III"}.}
}
\value{
A tibble with one row per model term and columns
\code{term}, \code{df}, \code{sumsq}, \code{meansq} (where available),
a test statistic (\code{statistic}, e.g. \emph{F}), and \code{p.value}.
}
\description{
Returns \strong{Type I}, \strong{II}, or \strong{III} sums-of-squares for an \code{aov} or
\code{lm} model as a tibble, leveraging \strong{car} and \strong{broom}.
}
\details{
\itemize{
\item \strong{Computation pathway}
\describe{
\item{Type I}{Calculated by applying \code{\link[broom:reexports]{broom::tidy()}} directly to the
fitted model object (\code{aov} or \code{lm}), which reproduces the sequential
sums-of-squares from \code{stats::anova()}.}
\item{Type II & III}{Calculated with \code{\link[car:Anova]{car::Anova()}}—using
\code{type = 'II'} or \code{type = 'III'}—and the resulting object is then converted
to a tibble via \code{\link[broom:reexports]{broom::tidy()}}.}
}
}
}
\examples{
fit <- aov(lincome ~ degree * sex, data = gss2021)
tidy_anova_ssq(fit) # default Type II
tidy_anova_ssq(fit, "I") # Type I
tidy_anova_ssq(fit, "III") # Type III
}

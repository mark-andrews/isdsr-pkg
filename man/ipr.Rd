% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptives.R
\name{ipr}
\alias{ipr}
\alias{idr}
\alias{itr}
\title{Inter-percentile range}
\usage{
ipr(x, p = 0.25, na.rm = TRUE, type = 7, ...)

idr(x, na.rm = FALSE, type = 7, ...)

itr(x, na.rm = FALSE, type = 7, ...)
}
\arguments{
\item{x}{Numeric vector.}

\item{p}{A proportion between 0 and 0.5 (exclusive).
Default \code{0.25} reproduces the inter-quartile range.}

\item{na.rm}{Logical; if \code{TRUE}, remove \code{NA}s before computing.}

\item{type}{Passed to \code{\link[stats:quantile]{stats::quantile()}}.  Default \code{7} matches \code{IQR()}.}

\item{...}{Additional arguments passed to \code{\link[stats:quantile]{stats::quantile()}}.}
}
\value{
A single numeric value: the difference between
\eqn{Q_{1 - p}} and \eqn{Q_{p}}. Returns \code{NA_real_} if the range is
undefined (e.g. all observations missing).
}
\description{
Computes the distance between the \eqn{p}-th and \eqn{(1 - p)}-th sample
percentiles.  \code{ipr()} is the general work-horse; \code{idr()} (inter-decile
range) and \code{itr()} (inter-tercile range) are thin wrappers that set
\eqn{p = 0.10} and \eqn{p = 1/3}, respectively.
}
\section{Functions}{
\itemize{
\item \code{idr()}: Inter-decile range (p = 0.10)

\item \code{itr()}: Inter-tercile range (p = 1/3)

}}
\examples{
x <- rnorm(100)
ipr(x) # same idea as IQR, but p = 0.25 by default
idr(x) # 10th–90th percentile range
itr(x) # 33.3rd–66.7th percentile range

}

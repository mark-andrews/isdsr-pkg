% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressions.R
\name{sprintf_term_fstat}
\alias{sprintf_term_fstat}
\title{Format an individual term’s F-test from a tidy ANOVA tibble}
\usage{
sprintf_term_fstat(anova_tbl, term_name, digits = 2)
}
\arguments{
\item{anova_tbl}{A tibble returned by \code{\link[=tidy_anova_ssq]{tidy_anova_ssq()}}.
Must contain columns \code{term}, \code{df}, \code{statistic}, and \code{p.value}.}

\item{digits}{Integer; number of decimal places to show for the
F statistic.  Defaults to 2.}

\item{term}{Character scalar. A value that appears in
\code{anova_tbl$term}; identifies which F-test to report.}
}
\value{
A length-one character vector such as
\code{"F(4, 72) = 3.52, p = .011"}.
}
\description{
Produces an APA-style string of the form
\code{"F(df1, df2) = f-value, p = .xxx"} for any term in a tibble
created by \code{\link[=tidy_anova_ssq]{tidy_anova_ssq()}}.
The numerator degrees of freedom and F statistic come from the
requested term’s row; the denominator degrees of freedom come from the
\code{"Residuals"} row.
}
\details{
\describe{
\item{Numerator df}{Taken from the \code{df} column of the specified term.}
\item{Denominator df}{Taken from the \code{df} value on the \code{"Residuals"}
row. The function errors if that row is missing.}
\item{F statistic & p-value}{From the \code{statistic} and \code{p.value}
columns of the specified term.}
}
}
\examples{
mod_tbl <- tidy_anova_ssq(aov(mpg ~ factor(cyl) * am, data = mtcars))
sprintf_term_fstat(mod_tbl, term_name = "factor(cyl)")
}

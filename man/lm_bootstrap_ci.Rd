% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regressions.R
\name{lm_bootstrap_ci}
\alias{lm_bootstrap_ci}
\title{Bootstrap percentile CIs for all lm coefficients and sigma using infer}
\usage{
lm_bootstrap_ci(model, reps = 5000, level = 0.95, seed = NULL)
}
\arguments{
\item{model}{A fitted \code{lm} object.}

\item{reps}{Number of bootstrap resamples (default 5000).}

\item{level}{Confidence level (default 0.95).}

\item{seed}{Optional integer for reproducibility.}
}
\value{
A tibble with rows for each coefficient and for \code{sigma},
columns: \code{term}, \code{estimate}, \code{lower}, \code{upper}.
}
\description{
Bootstrap percentile CIs for all lm coefficients and sigma using infer
}
\examples{
fit <- lm(mpg ~ wt + hp, data = mtcars)
lm_bootstrap_ci(fit, reps = 2000, seed = 1)
}

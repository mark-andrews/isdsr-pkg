% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptives.R
\name{get_binned_mode}
\alias{get_binned_mode}
\title{Approximate the mode of a continuous variable by binning}
\usage{
get_binned_mode(data, var, n_bins = 10)
}
\arguments{
\item{data}{A data frame.}

\item{var}{A numeric column of the data frame.}

\item{n_bins}{Integer, number of equal-width bins to create.  Defaults to 10.}
}
\value{
A tibble with two columns:
\describe{
\item{\code{var}}{the midpoint of the bin with the highest count;}
\item{\code{count}}{the number of observations in that bin.}
}
}
\description{
\code{get_binned_mode()} divides a numeric variable into equal-width bins
(default = 10), counts the observations in each bin, and returns the
midpoint of the fullest bin.  This illustrates why the mode is usually
unhelpful for truly continuous data: the result depends entirely on how
you choose the bins.
}
\examples{
## Approximate mode of the happiness scores (0–10 scale)
get_binned_mode(whr2025, happiness, n_bins = 5)

## same idea applied to raw GDP (skewed, wide-range data)
get_binned_mode(whr2025, gdp, n_bins = 20)

}

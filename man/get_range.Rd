% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptives.R
\name{get_range}
\alias{get_range}
\title{Range width (max minus min)}
\usage{
get_range(x, na.rm = TRUE)
}
\arguments{
\item{x}{Numeric vector.}

\item{na.rm}{Logical; if \code{TRUE} (default) remove \code{NA} values before
computing the range.}
}
\value{
A single numeric value \code{max(x) - min(x)}, or \code{NA_real_} when the
range is undefined.
}
\description{
Computes the distance between the smallest and largest finite values in a
numeric vector.  If the vector is empty, contains only \code{NA}s (and
\code{na.rm = TRUE}), or contains only \code{Inf}/\code{-Inf}, the function returns
\code{NA_real_}.
}
\examples{
get_range(c(3, 10, 7, 4)) # 7
get_range(42) # 0
get_range(c(2, NA, 8)) # 6
get_range(c(2, NA, 8), na.rm = FALSE) # NA
get_range(numeric(0)) # NA
get_range(c(Inf, -Inf)) # NA

}
